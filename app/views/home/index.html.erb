<div class="w-full">
  <div class="bg-white p-6 rounded-lg shadow-lg">
    <h1 class="text-2xl font-bold mb-4">Welcome to QR Code Inventory Management</h1>
    
    <div class="mb-4">
      <p class="text-gray-600">You are signed in as: <span class="font-semibold"><%= current_user.email %></span></p>
    </div>

    <div class="mt-4">
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>
</div>
